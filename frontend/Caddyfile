{
	admin off
	auto_https off
	persist_config off
}

:{$PORT:3000} {
	encode zstd gzip

	handle /health {
		respond "ok" 200
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
